# Contexts
Filter context: it is the set of attributes that filter the model before the expression is evaluated.
Row context: it is the iteration row by row performed by certain expressions.
Context transition: a CALCULATE function defined in a row context transforms the row values into filters.



